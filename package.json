{
  "name": "unkey-hono-cf-ratelimit",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "@unkey/ratelimit": "^0.4.5",
    "hono": "^4.6.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.79.0"
  }
}
